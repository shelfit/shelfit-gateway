Book:
  type: object
  config:
    fields:
      id: "Int!"
      title: "String!"
      author: "String!"
      genres: "[String]"
      pageCount: "String!"
      coverUrl: "String"
      description: "String"
      source: "String!"
      visibility: "String!"
