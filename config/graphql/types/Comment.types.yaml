Comment:
  type: object
  config:
    fields:
      id: "Int!"
      user: "User!"
      text: "String!"
      depth: "Int!"
      numLikes: "Int!"
      numReplies: "Int!"
      parentId: "Int"
      createdAt: "DateTimeImmutable!"

CommentThread:
  type: object
  config:
    fields:
      root: "Comment!"
      replies: "[Comment]!"
      hasMoreReplies: "Boolean!"

CreateCommentInput:
  type: input-object
  config:
    fields:
      text: "String!"
      postId: "Int!"
      parentId: "Int"